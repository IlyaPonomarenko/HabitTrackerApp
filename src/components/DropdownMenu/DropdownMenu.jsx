import React from "react";

const DropdownMenu = (
  options = ["Very Important", "Important", "Normal", "Not Important"]
) => {
  return <div className="dropdown-menu"></div>;
};

export default DropdownMenu;
